-- The tree ships uv_build 0.12.0; upstream pins <0.12. Relax the upper bound.

--- pyproject.toml.orig	2026-08-21 00:00:00 UTC
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["uv_build>=0.11.14,<0.12"]
+requires = ["uv_build>=0.11.14"]
 build-backend = "uv_build"
 
 [tool.uv.build-backend]
