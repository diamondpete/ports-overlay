--- pyproject.toml.orig	2026-09-18 03:44:33 UTC
+++ pyproject.toml
@@ -3,7 +3,7 @@ requires = [
 
 [build-system]
 requires = [
-    "setuptools == 78.1.1",
+    "setuptools",
     "wheel",
 ]
 build-backend = "setuptools.build_meta"
