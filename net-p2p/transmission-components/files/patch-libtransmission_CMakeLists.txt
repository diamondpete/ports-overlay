--- libtransmission/CMakeLists.txt.orig	2026-01-26 23:59:25 UTC
+++ libtransmission/CMakeLists.txt
@@ -55,7 +55,7 @@ target_sources(${TR_NAME}
         error-types.h
         error.cc
         error.h
-        file-capacity.cc
+	file-capacity.cc
         file-piece-map.cc
         file-piece-map.h
         file-posix.cc
@@ -227,6 +227,8 @@ target_compile_definitions(${TR_NAME}
         $<$<BOOL:${USE_SYSTEM_B64}>:USE_SYSTEM_B64>
         $<$<BOOL:${HAVE_SO_REUSEPORT}>:HAVE_SO_REUSEPORT=1>
     PUBLIC
+        FMT_USE_EXCEPTIONS=0 # {fmt} >= 11.2.0
+        FMT_EXCEPTIONS=0 # {fmt} < 11.2.0
         $<$<STREQUAL:${CRYPTO_PKG},ccrypto>:WITH_CCRYPTO>
         $<$<STREQUAL:${CRYPTO_PKG},mbedtls>:WITH_MBEDTLS>
         $<$<STREQUAL:${CRYPTO_PKG},openssl>:WITH_OPENSSL>
@@ -235,9 +237,9 @@ tr_target_compile_definitions_for_headers(${TR_NAME}
 
 tr_target_compile_definitions_for_headers(${TR_NAME}
     PRIVATE
-        sys/statvfs.h
+    	sys/statvfs.h
         xfs/xfs.h
-        xlocale.h)
+	xlocale.h)
 
 tr_target_compile_definitions_for_functions(${TR_NAME}
     PRIVATE
@@ -245,7 +247,7 @@ tr_target_compile_definitions_for_functions(${TR_NAME}
         copy_file_range
         fallocate64
         flock
-        getmntent
+	getmntent
         htonll
         mkdtemp
         ntohll
@@ -254,7 +256,7 @@ tr_target_compile_definitions_for_functions(${TR_NAME}
         pread
         pwrite
         sendfile64
-        statvfs)
+	statvfs)
 
 target_include_directories(${TR_NAME}
     PUBLIC
@@ -269,12 +271,12 @@ target_link_libraries(${TR_NAME}
 target_link_libraries(${TR_NAME}
     PRIVATE
         Threads::Threads
-        deflate::deflate
+        libdeflate::libdeflate
         CURL::libcurl
         FastFloat::fast_float
         psl::psl
         natpmp::natpmp
-        miniupnpc::libminiupnpc
+        miniupnpc::miniupnpc
         dht::dht
         libutp::libutp
         libb64::libb64
@@ -285,7 +287,7 @@ target_link_libraries(${TR_NAME}
         RapidJSON
         utf8::cpp
         wildmat
-        WideInteger::WideInteger
+        transmission::WideInteger
         Crc32c::crc32c
         $<$<BOOL:${WIN32}>:crypt32>
         $<$<BOOL:${WIN32}>:shlwapi>
@@ -295,7 +297,7 @@ target_link_libraries(${TR_NAME}
     PUBLIC
         transmission::crypto_impl
         fmt::fmt-header-only
-        small::small
+        transmission::small
         libevent::event)
 
 if(INSTALL_LIB)
